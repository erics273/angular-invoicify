// import { Client } from './client';

describe(`Client`, () => {

  /*
  it(`should have a constructor that takes an id, a name, and an optional
     flag that describes if the client is active.`, () => {
    expect(() => new Client(1, 'Spiegelhaus')).not.toThrow();
    expect(() => new Client(1, 'Spiegelhaus', true)).not.toThrow();
  });
  */

  /*
  it(`should be inactive if not specified in the constructor`, () => {
    const client = new Client(1, 'Spiegelhaus');
    expect(client.active).toBeFalsy();
  });
  */

  /*
  it(`should have a toJSON object that returns a ClientDTO`, () => {
    const client = new Client(1, 'Spiegelhaus', true);
    const jsonObject = client.toJSON();
    expect(jsonObject.id).toBe(1);
    expect(jsonObject.name).toBe('Spiegelhaus');
    expect(jsonObject.active).toBeTruthy();
  });
  */

  /*
  it(`should have getter properties for id, name, and active`, () => {
    const client = new Client(1, 'Spiegelhaus', true);
    expect(client.id).toBe(1);
    expect(client.name).toBe('Spiegelhaus');
    expect(client.active).toBeTruthy();
  });
  */

  /*
  it(`should have an activate method that makes the client active`, () => {
    const client = new Client(1, 'Spiegelhaus');
    client.activate();
    expect(client.active).toBeTruthy();
  });
  */

  /*
  it(`should have a deactivate method that makes the client deactive`, () => {
    const client = new Client(1, 'Spiegelhaus', true);
    client.deactivate();
    expect(client.active).toBeFalsy();
  });
  */

});
