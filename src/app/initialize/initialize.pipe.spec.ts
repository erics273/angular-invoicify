import { InitializePipe } from './initialize.pipe';

describe('InitializePipe', () => {
  it('create an instance', () => {
    const pipe = new InitializePipe();
    expect(pipe).toBeTruthy();
  });
});
